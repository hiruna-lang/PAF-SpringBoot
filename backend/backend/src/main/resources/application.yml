server:
  port: 8090

spring:
  application:
    name: backend
  datasource:
    url: jdbc:mysql://localhost:3306/WorkLancer?createDatabaseIfNotExist=true
    username: root
    password: Awantha65@
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 30MB

module3:
  upload-dir: uploads/module3
